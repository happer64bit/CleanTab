{
  "dependencies": {
    "parcel": "^2.5.0",
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  },
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --dist-dir bundle && echo 'Successfully bundled into bundle/ folder'"
  },
  "devDependencies": {
    "process": "^0.11.10"
  }
}
